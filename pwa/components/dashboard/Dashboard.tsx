import React from 'react';

type DashboardProps = {
};

const Dashboard: React.FC<DashboardProps> = ({  }) => {
    return (
        <>
            <h1>Dashboard</h1>
        </>
    );
};

export default Dashboard;
